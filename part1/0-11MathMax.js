const max1 = Math.max(1,2,3)
const max2 = Math.max(2,4,4)

console.log(max1)
console.log(max2)
// lol why have I had the need to check that - no idea. Perhaps because I am in chaos and hurry and it was easier to "just do stuff" than to think. 
//I don't like it, but at least I am moving forward (instead of waiting for better conditions)