Create a similar diagram depicting the situation where the user creates a new note on the page https://studies.cs.helsinki.fi/exampleapp/notes by writing something into the text field and clicking the submit button.

If necessary, show operations on the browser or on the server as comments on the diagram.

The diagram does not have to be a sequence diagram. Any sensible way of presenting the events is fine.




button is clicked:

-1. browser->server: sending the data (note) using HTTP POST (at url specified in form's action field) 
server->browser: http status code 302 redirect (to oneself) [server asks browser to do new http get]

1. browser-> server: fetching the html from the server using HTTP GET request
2. server -> browser: sending the HTML document from the url

3. browser: parsing html.
    4. Found css link. browser-> server: fetch the css from the server using HTTP GET request.
    5. server->browser: sending the css file from the url
6. parsing html further
    7. Found script "link". browser->server: fetch the js file from the server using HTTP GET request.
    8. server->Browser: sending the js file
        9.browser executing js code
            10. found need to fetch some file in js code. browser->server: fetch that file from the server using HTTP GET request.
            11. server->browser: sending the file from the server
            12. browser conitnues to execute js code - executes the callback function in js file that renders the notes  (it was if-walled after data is fetched).




